import UWS from '../dist';

// const app = new App();

// app.get('/', (req, res) => {
//   res.json({ message: 'Home' });
// });

// app.listen(3000, success => {
//   console.log(success ? 'Server running on http://localhost:3000' : 'Failed to start server');
// });


conssole.log('UWS:', UWS);