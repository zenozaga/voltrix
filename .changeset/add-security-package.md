---
"@voltrix/security": minor
"@voltrix/core": minor
---

Introduce the brand new `@voltrix/security` package, providing 6 high-performance security modules (IP Filter, CORS, Helmet, Rate Limiter, CSRF, and Sessions) natively optimized for Voltrix's zero-allocation architecture.
Add `setHeader` method to the core `IResponse` interface definition in `@voltrix/core`.
