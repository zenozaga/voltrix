# Changesets

This directory contains the configurations and temporary files used by Changesets to track package version bumps and generate changelogs.

Do not delete or manually modify files in this directory except for configuration updates in `config.json`.
